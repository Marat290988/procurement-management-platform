import { CSSProperties } from "react";

export const cellStyleHeader: CSSProperties = {
  fontWeight: '700',
  backgroundColor: 'var(--pink)',
}

export const cellStyleBody: CSSProperties = {
  justifyContent: 'center',
  padding: '2px',
  display: 'flex',
  alignItems: 'center',
}